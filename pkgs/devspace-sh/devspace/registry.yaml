# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: devspace-sh
    repo_name: devspace
    aliases:
      - name: loft-sh/devspace
    description: "DevSpace - The Fastest Developer Tool for Kubernetes  Automate your deployment workflow with DevSpace and develop software directly inside Kubernetes"
    version_constraint: "false"
    version_overrides:
      - version_constraint: Version == "v6.2.0-alpha.0"
        no_asset: true
      - version_constraint: semver("<= 5.7.4")
        type: http
        url: https://gh-proxy.org/github.com/devspace-sh/devspace/releases/download/{{.Version}}/devspace-{{.OS}}-{{.Arch}}
        asset: devspace-{{.OS}}-{{.Arch}}
        format: raw
        rosetta2: true
        windows_arm_emulation: true
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: semver("<= 5.10.0")
        type: http
        url: https://gh-proxy.org/github.com/devspace-sh/devspace/releases/download/{{.Version}}/devspace-{{.OS}}-{{.Arch}}
        asset: devspace-{{.OS}}-{{.Arch}}
        format: raw
        windows_arm_emulation: true
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/devspace-sh/devspace/releases/download/{{.Version}}/devspace-{{.OS}}-{{.Arch}}
        asset: devspace-{{.OS}}-{{.Arch}}
        format: raw
        windows_arm_emulation: true
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
