# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: yorukot
    repo_name: superfile
    aliases:
      - name: MHNightCat/superfile
    description: Pretty fancy and modern terminal file manager
    version_constraint: "false"
    version_overrides:
      - version_constraint: Version == "v1.0.0"
        type: http
        url: https://gh-proxy.org/github.com/yorukot/superfile/releases/download/{{.Version}}/spf
        asset: spf
        format: raw
        files:
          - name: spf
        supported_envs:
          - linux/amd64
      # Windows assets are missing only in this release. v1.1.7.1 has them.
      - version_constraint: Version == "v1.1.7"
        type: http
        url: https://gh-proxy.org/github.com/yorukot/superfile/releases/download/{{.Version}}/superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
        asset: superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
        format: tar.gz
        files:
          - name: spf
            src: dist/{{.AssetWithoutExt}}/spf
        supported_envs:
          - linux
          - darwin
      - version_constraint: semver("<= 1.1.1")
        type: http
        url: https://gh-proxy.org/github.com/yorukot/superfile/releases/download/{{.Version}}/superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
        asset: superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
        format: tar.gz
        files:
          - name: spf
            src: dist/{{.AssetWithoutExt}}/spf
        supported_envs:
          - linux
          - darwin
      - version_constraint: semver("<= 1.6.0-rc1")
        type: http
        url: https://gh-proxy.org/github.com/yorukot/superfile/releases/download/{{.Version}}/superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
        asset: superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
        format: tar.gz
        files:
          - name: spf
            src: dist/{{.AssetWithoutExt}}/spf
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/yorukot/superfile/releases/download/{{.Version}}/superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/yorukot/superfile/releases/download/{{.Version}}/superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
        asset: superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
        format: tar.gz
        files:
          - name: spf
            src: dist/{{.AssetWithoutExt}}/spf
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/yorukot/superfile/releases/download/{{.Version}}/superfile-{{.Version}}-checksums.txt
          asset: superfile-{{.Version}}-checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/yorukot/superfile/releases/download/{{.Version}}/superfile-{{.OS}}-{{.Version}}-{{.Arch}}.{{.Format}}
            format: zip
