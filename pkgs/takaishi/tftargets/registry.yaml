# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: takaishi
    repo_name: tftargets
    description: A tool that analyzes Terraform configurations and identifies directories that need to be executed based on Git changes
    version_constraint: "false"
    version_overrides:
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/takaishi/tftargets/releases/download/{{.Version}}/tftargets_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: tftargets_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/takaishi/tftargets/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
