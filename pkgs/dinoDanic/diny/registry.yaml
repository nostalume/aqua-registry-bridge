# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: dinoDanic
    repo_name: diny
    description: generate git commit messages
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.1.2")
        no_asset: true
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/dinoDanic/diny/releases/download/{{.Version}}/diny_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: diny_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/dinoDanic/diny/releases/download/{{.Version}}/diny_{{trimV .Version}}_checksums.txt
          asset: diny_{{trimV .Version}}_checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/dinoDanic/diny/releases/download/{{.Version}}/diny_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
