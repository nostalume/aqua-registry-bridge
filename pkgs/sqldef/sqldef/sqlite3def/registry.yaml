# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - name: sqldef/sqldef/sqlite3def
    aliases:
      - name: k0kubun/sqldef/sqlite3def
    type: http
    url: https://gh-proxy.org/github.com/sqldef/sqldef/releases/download/{{.Version}}/sqlite3def_{{.OS}}_{{.Arch}}.{{.Format}}
    repo_owner: sqldef
    repo_name: sqldef
    description: Idempotent schema management for SQLite3
    asset: sqlite3def_{{.OS}}_{{.Arch}}.{{.Format}}
    files:
      - name: sqlite3def
    format: zip
    overrides:
      - goos: linux
        type: http
        url: https://gh-proxy.org/github.com/sqldef/sqldef/releases/download/{{.Version}}/sqlite3def_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
    supported_envs:
      - darwin
      - linux
      - windows/amd64
