# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: http
    url: https://gh-proxy.org/github.com/mitchellh/gon/releases/download/{{.Version}}/gon_{{.OS}}.zip
    repo_owner: mitchellh
    repo_name: gon
    asset: gon_{{.OS}}.zip
    description: Sign, notarize, and package macOS CLI tools and applications written in any language. Available as both a CLI and a Go library
    replacements:
      darwin: macos
    supported_envs:
      - darwin
    rosetta2: true
