# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: kbrdn1
    repo_name: gwm-cli
    description: git worktree manager — Rust CLI + ratatui TUI, native libgit2, per-repo bootstrap via .gwm.toml
    files:
      - name: gwm
    version_constraint: "false"
    version_overrides:
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/kbrdn1/gwm-cli/releases/download/{{.Version}}/gwm-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
        asset: gwm-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
        format: tar.gz
        files:
          - name: gwm
            src: "{{.AssetWithoutExt}}/gwm"
        windows_arm_emulation: true
        replacements:
          amd64: x86_64
          arm64: aarch64
          darwin: apple-darwin
          linux: unknown-linux-gnu
          windows: pc-windows-msvc
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/kbrdn1/gwm-cli/releases/download/{{.Version}}/gwm-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
            format: zip
