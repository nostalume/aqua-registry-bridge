# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: dadav
    repo_name: helm-schema
    description: Generate jsonschemas from helm charts
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.4.2")
        type: http
        url: https://gh-proxy.org/github.com/dadav/helm-schema/releases/download/{{.Version}}/helm-schema_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: helm-schema_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/dadav/helm-schema/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/dadav/helm-schema/releases/download/{{.Version}}/helm-schema_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: Version == "0.4.3"
        type: http
        url: https://gh-proxy.org/github.com/dadav/helm-schema/releases/download/{{.Version}}/helm-schema_.{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: helm-schema_.{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        # Checksum file is broken
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/dadav/helm-schema/releases/download/{{.Version}}/helm-schema_.{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/dadav/helm-schema/releases/download/{{.Version}}/helm-schema_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: helm-schema_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/dadav/helm-schema/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/dadav/helm-schema/releases/download/{{.Version}}/helm-schema_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
