# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: tedilabs
    repo_name: tfvault
    description: A Terraform credentials helper to fetch secrets from several secret backends (OS keyring, environment variables, pass)
    version_constraint: "false"
    version_overrides:
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/tedilabs/tfvault/releases/download/{{.Version}}/tfvault_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: tfvault_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/tedilabs/tfvault/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        supported_envs:
          - linux
          - darwin
