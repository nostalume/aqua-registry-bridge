# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: tedilabs
    repo_name: ota
    description: k9s for Okta — a Go/Bubbletea TUI to inspect Okta admin resources
    version_constraint: "false"
    version_overrides:
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/tedilabs/ota/releases/download/{{.Version}}/ota_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: ota_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        windows_arm_emulation: true
        replacements:
          darwin: macos
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/tedilabs/ota/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/tedilabs/ota/releases/download/{{.Version}}/ota_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
