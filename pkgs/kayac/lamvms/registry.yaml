# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: kayac
    repo_name: lamvms
    description: lamvms is a deployment and lifecycle management tool for AWS Lambda MicroVMs
    version_constraint: "false"
    version_overrides:
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/kayac/lamvms/releases/download/{{.Version}}/lamvms_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: lamvms_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/kayac/lamvms/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        supported_envs:
          - linux
          - darwin
