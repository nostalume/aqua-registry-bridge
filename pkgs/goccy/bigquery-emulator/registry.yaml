# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: goccy
    repo_name: bigquery-emulator
    description: BigQuery emulator server implemented in Go
    version_constraint: "false"
    version_overrides:
      - version_constraint: Version == "v0.1.2"
        type: http
        url: https://gh-proxy.org/github.com/goccy/bigquery-emulator/releases/download/{{.Version}}/bigquery-emulator-{{.OS}}-{{.Arch}}
        asset: bigquery-emulator-{{.OS}}-{{.Arch}}
        format: raw
        rosetta2: true
        supported_envs:
          - darwin
      - version_constraint: semver("< 0.7.0")
        type: http
        url: https://gh-proxy.org/github.com/goccy/bigquery-emulator/releases/download/{{.Version}}/bigquery-emulator-{{.OS}}-{{.Arch}}
        asset: bigquery-emulator-{{.OS}}-{{.Arch}}
        format: raw
        rosetta2: true
        supported_envs:
          - linux/amd64
          - darwin
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/goccy/bigquery-emulator/releases/download/{{.Version}}/bigquery-emulator_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: bigquery-emulator_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        rosetta2: true
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/goccy/bigquery-emulator/releases/download/{{.Version}}/bigquery-emulator_{{.Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
        github_artifact_attestations:
          signer_workflow: goccy/bigquery-emulator/.github/workflows/release.yml
