# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: dahlia
    repo_name: hongdown
    description: A Markdown formatter that enforces Hong Minhee's Markdown style conventions
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.4.2")
        type: http
        url: https://gh-proxy.org/github.com/dahlia/hongdown/releases/download/{{.Version}}/hongdown-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
        asset: hongdown-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
        format: tar.bz2
        files:
          - name: hongdown
            src: hongdown-{{.Arch}}-{{.OS}}/hongdown
        replacements:
          amd64: x86_64
          arm64: aarch64
          darwin: apple-darwin
          linux: unknown-linux-musl
          windows: pc-windows-msvc
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/dahlia/hongdown/releases/download/{{.Version}}/hongdown-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
            format: zip
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/dahlia/hongdown/releases/download/{{.Version}}/hongdown-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
        asset: hongdown-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
        format: tar.bz2
        files:
          - name: hongdown
        replacements:
          amd64: x86_64
          arm64: aarch64
          darwin: apple-darwin
          linux: unknown-linux-musl
          windows: pc-windows-msvc
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/dahlia/hongdown/releases/download/{{.Version}}/hongdown-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
            format: zip
