# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: entireio
    repo_name: cli
    description: Entire CLI hooks into your Git workflow to capture AI agent sessions as you work. Sessions are indexed alongside commits, creating a searchable record of how code was written in your repo
    files:
      - name: entire
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.3.9")
        type: http
        url: https://gh-proxy.org/github.com/entireio/cli/releases/download/{{.Version}}/cli_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: cli_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/entireio/cli/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        supported_envs:
          - linux
          - darwin
      - version_constraint: semver("<= 0.5.1")
        type: http
        url: https://gh-proxy.org/github.com/entireio/cli/releases/download/{{.Version}}/entire_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: entire_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/entireio/cli/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        supported_envs:
          - linux
          - darwin
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/entireio/cli/releases/download/{{.Version}}/entire_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: entire_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/entireio/cli/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/entireio/cli/releases/download/{{.Version}}/entire_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
