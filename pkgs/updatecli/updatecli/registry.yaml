# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: updatecli
    repo_name: updatecli
    description: A Declarative Dependency Management tool
    version_constraint: "false"
    version_overrides:
      - version_constraint: Version in ["v0.0.18", "v0.0.32"]
        no_asset: true
      - version_constraint: Version == "v0.0.23"
        type: http
        url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli.amd64.exe
        asset: updatecli.amd64.exe
        format: raw
        supported_envs:
          - windows
      - version_constraint: semver("<= 0.0.5")
        no_asset: true
      - version_constraint: semver("<= 0.0.10")
        type: http
        url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli
        asset: updatecli
        format: raw
        supported_envs:
          - linux/amd64
      - version_constraint: semver("<= 0.3.0")
        type: http
        url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli.{{.OS}}.{{.Arch}}
        asset: updatecli.{{.OS}}.{{.Arch}}
        format: raw
        rosetta2: true
        windows_arm_emulation: true
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli.{{.Arch}}
            asset: updatecli.{{.Arch}}
        supported_envs:
          - darwin
          - windows
          - amd64
      - version_constraint: semver("<= 0.7.1")
        type: http
        url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        windows_arm_emulation: true
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{trimV .Version}}_checksums.txt
          asset: updatecli_{{trimV .Version}}_checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: semver("<= 0.40.1")
        type: http
        url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{trimV .Version}}_checksums.txt
          asset: updatecli_{{trimV .Version}}_checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: semver("<= 0.110.3")
        type: http
        url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
          cosign:
            opts:
              - --certificate
              - https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/checksums.txt.pem
              - --certificate-identity
              - "https://gh-proxy.org/github.com/updatecli/updatecli/.github/workflows/release.yaml@refs/tags/{{.Version}}"
              - --certificate-oidc-issuer
              - https://token.actions.githubusercontent.com
              - --signature
              - https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/checksums.txt.sig
              - --certificate-github-workflow-repository
              - updatecli/updatecli
              - --certificate-github-workflow-ref
              - refs/tags/{{.Version}}
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: semver("<= 0.114.0")
        type: http
        url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
          cosign:
            bundle:
              type: http
              url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/checksums.txt.sig
              asset: checksums.txt.sig
            opts:
              - --certificate-identity
              - "https://gh-proxy.org/github.com/updatecli/updatecli/.github/workflows/release.yaml@refs/tags/{{.Version}}"
              - --certificate-oidc-issuer
              - https://token.actions.githubusercontent.com
              - --certificate-github-workflow-repository
              - updatecli/updatecli
              - --certificate-github-workflow-ref
              - refs/tags/{{.Version}}
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
          cosign:
            bundle:
              type: http
              url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/checksums.txt.sig
              asset: checksums.txt.sig
            opts:
              - --certificate-identity
              - "https://gh-proxy.org/github.com/updatecli/updatecli/.github/workflows/release.yaml@refs/tags/{{.Version}}"
              - --certificate-oidc-issuer
              - https://token.actions.githubusercontent.com
              - --certificate-github-workflow-repository
              - updatecli/updatecli
              - --certificate-github-workflow-ref
              - refs/tags/{{.Version}}
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/updatecli/updatecli/releases/download/{{.Version}}/updatecli_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
        github_artifact_attestations:
          signer_workflow: updatecli/updatecli/.github/workflows/release.yaml
