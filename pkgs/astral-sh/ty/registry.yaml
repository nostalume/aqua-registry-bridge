# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: astral-sh
    repo_name: ty
    description: An extremely fast Python type checker and language server, written in Rust
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.0.23")
        type: http
        url: https://gh-proxy.org/github.com/astral-sh/ty/releases/download/{{.Version}}/ty-{{.Arch}}-{{.OS}}.{{.Format}}
        asset: ty-{{.Arch}}-{{.OS}}.{{.Format}}
        format: tar.gz
        files:
          - name: ty
            src: ty-{{.Arch}}-{{.OS}}/ty
        replacements:
          amd64: x86_64
          arm64: aarch64
          darwin: apple-darwin
          linux: unknown-linux-musl
          windows: pc-windows-msvc
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/astral-sh/ty/releases/download/{{.Version}}/ty-{{.Arch}}-{{.OS}}.{{.Format}}
            format: zip
            files:
              - name: ty
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/astral-sh/ty/releases/download/{{.Version}}/ty-{{.Arch}}-{{.OS}}.{{.Format}}
        asset: ty-{{.Arch}}-{{.OS}}.{{.Format}}
        format: tar.gz
        files:
          - name: ty
            src: ty-{{.Arch}}-{{.OS}}/ty
        replacements:
          amd64: x86_64
          arm64: aarch64
          darwin: apple-darwin
          linux: unknown-linux-musl
          windows: pc-windows-msvc
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/astral-sh/ty/releases/download/{{.Version}}/ty-{{.Arch}}-{{.OS}}.{{.Format}}
            format: zip
            files:
              - name: ty
        github_artifact_attestations:
          signer_workflow: astral-sh/ty/.github/workflows/release.yml
