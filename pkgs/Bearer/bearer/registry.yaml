# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: Bearer
    repo_name: bearer
    description: Code security scanning tool (SAST) to discover, filter and prioritize security and privacy risks
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.24.0")
        no_asset: true
      - version_constraint: semver("<= 1.43.4")
        type: http
        url: https://gh-proxy.org/github.com/Bearer/bearer/releases/download/{{.Version}}/bearer_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: bearer_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/Bearer/bearer/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        overrides:
          - goos: linux
            type: http
            url: https://gh-proxy.org/github.com/Bearer/bearer/releases/download/{{.Version}}/bearer_{{trimV .Version}}_{{.OS}}-{{.Arch}}.pkg.{{.Format}}
            format: tar.zst
            asset: bearer_{{trimV .Version}}_{{.OS}}-{{.Arch}}.pkg.{{.Format}}
            files:
              - name: bearer
                src: usr/bin/bearer
        supported_envs:
          - linux/amd64
          - darwin
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/Bearer/bearer/releases/download/{{.Version}}/bearer_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: bearer_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/Bearer/bearer/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        overrides:
          - goos: linux
            type: http
            url: https://gh-proxy.org/github.com/Bearer/bearer/releases/download/{{.Version}}/bearer_{{trimV .Version}}_{{.OS}}-{{.Arch}}.pkg.{{.Format}}
            format: tar.zst
            asset: bearer_{{trimV .Version}}_{{.OS}}-{{.Arch}}.pkg.{{.Format}}
            files:
              - name: bearer
                src: usr/bin/bearer
        supported_envs:
          - linux
          - darwin
