# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: akiomik
    repo_name: mado
    description: A fast Markdown linter written in Rust
    version_constraint: "false"
    version_overrides:
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/akiomik/mado/releases/download/{{.Version}}/mado-{{.OS}}-{{.Arch}}.{{.Format}}
        asset: mado-{{.OS}}-{{.Arch}}.{{.Format}}
        format: tar.gz
        windows_arm_emulation: true
        replacements:
          amd64: x86_64
          darwin: macOS
          linux: Linux-gnu
          windows: Windows
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/akiomik/mado/releases/download/{{.Version}}/mado-{{.OS}}-msvc-{{.Arch}}.{{.Format}}
            format: zip
            asset: mado-{{.OS}}-msvc-{{.Arch}}.{{.Format}}
