# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: FairwindsOps
    repo_name: polaris
    description: Validation of best practices in your Kubernetes clusters
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("< 10.2.0")
        type: http
        url: https://gh-proxy.org/github.com/FairwindsOps/polaris/releases/download/{{.Version}}/polaris_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: polaris_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/FairwindsOps/polaris/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/FairwindsOps/polaris/releases/download/{{.Version}}/polaris_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: polaris_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        supported_envs:
          - darwin
          - linux
          - windows/amd64
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/FairwindsOps/polaris/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
          cosign:
            bundle:
              type: http
              url: https://gh-proxy.org/github.com/FairwindsOps/polaris/releases/download/{{.Version}}/checksums.txt.sigstore.json
              asset: checksums.txt.sigstore.json
            opts:
              - --key
              - https://artifacts.fairwinds.com/cosign-p256.pub
