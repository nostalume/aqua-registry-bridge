# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: Julien-R44
    repo_name: fast-ssh
    description: FastSSH is a TUI that allows you to quickly connect to your services by navigating through your SSH config
    version_constraint: "false"
    version_overrides:
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/Julien-R44/fast-ssh/releases/download/{{.Version}}/fast-ssh-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
        asset: fast-ssh-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
        format: tar.gz
        rosetta2: true
        windows_arm_emulation: true
        replacements:
          amd64: x86_64
          darwin: apple-darwin
          linux: unknown-linux-gnu
          windows: pc-windows-msvc
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/Julien-R44/fast-ssh/releases/download/{{.Version}}/fast-ssh-{{.Version}}-{{.Arch}}-{{.OS}}.{{.Format}}
            format: zip
        supported_envs:
          - darwin
          - windows
          - amd64
