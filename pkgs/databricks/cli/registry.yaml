# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: databricks
    repo_name: cli
    description: Databricks CLI
    files:
      - name: databricks
    version_constraint: "false"
    version_overrides:
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/databricks/cli/releases/download/{{.Version}}/databricks_cli_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: databricks_cli_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/databricks/cli/releases/download/{{.Version}}/databricks_cli_{{trimV .Version}}_SHA256SUMS
          asset: databricks_cli_{{trimV .Version}}_SHA256SUMS
          algorithm: sha256
