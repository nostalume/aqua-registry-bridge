# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: bellicose100xp
    repo_name: jiq
    description: Interactive JSON query tool with real-time output and AI assistance
    version_constraint: "false"
    version_overrides:
      - version_constraint: Version == "v2.0.0"
        no_asset: true
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/bellicose100xp/jiq/releases/download/{{.Version}}/jiq-{{.Arch}}-{{.OS}}.{{.Format}}
        asset: jiq-{{.Arch}}-{{.OS}}.{{.Format}}
        files:
          - name: jiq
            src: "{{.AssetWithoutExt}}/jiq"
        format: tar.xz
        windows_arm_emulation: true
        replacements:
          amd64: x86_64
          arm64: aarch64
          darwin: apple-darwin
          windows: pc-windows-msvc
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
        overrides:
          - goos: linux
            type: http
            url: https://gh-proxy.org/github.com/bellicose100xp/jiq/releases/download/{{.Version}}/jiq-{{.Arch}}-{{.OS}}.{{.Format}}
            goarch: amd64
            replacements:
              linux: unknown-linux-musl
          - goos: linux
            type: http
            url: https://gh-proxy.org/github.com/bellicose100xp/jiq/releases/download/{{.Version}}/jiq-{{.Arch}}-{{.OS}}.{{.Format}}
            goarch: arm64
            replacements:
              linux: unknown-linux-gnu
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/bellicose100xp/jiq/releases/download/{{.Version}}/jiq-{{.Arch}}-{{.OS}}.{{.Format}}
            format: zip
            files:
              - name: jiq
