# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: relusc
    repo_name: hclconv
    description: Simple tool to convert JSON files to HCL2 files and vice versa
    version_constraint: "false"
    version_overrides:
      - version_constraint: Version == "v0.1.0"
        type: http
        url: https://gh-proxy.org/github.com/relusc/hclconv/releases/download/{{.Version}}/hclconv_{{.OS}}_{{.Arch}}_{{.Version}}.{{.Format}}
        asset: hclconv_{{.OS}}_{{.Arch}}_{{.Version}}.{{.Format}}
        format: tar.gz
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/relusc/hclconv/releases/download/{{.Version}}/hclconv_{{.OS}}_{{.Arch}}_{{.Version}}.{{.Format}}
        asset: hclconv_{{.OS}}_{{.Arch}}_{{.Version}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/relusc/hclconv/releases/download/{{.Version}}/hclconv_{{trimV .Version}}_checksums.txt
          asset: hclconv_{{trimV .Version}}_checksums.txt
          algorithm: sha256
