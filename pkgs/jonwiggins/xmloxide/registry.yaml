# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: jonwiggins
    repo_name: xmloxide
    description: A pure Rust reimplementation of libxml2
    files:
      - name: xmllint
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.4.1")
        no_asset: true
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/jonwiggins/xmloxide/releases/download/{{.Version}}/xmllint_{{.OS}}-{{.Arch}}
        asset: xmllint_{{.OS}}-{{.Arch}}
        format: raw
        windows_arm_emulation: true
        replacements:
          arm64: aarch64
          amd64: x86-64
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/jonwiggins/xmloxide/releases/download/{{.Version}}/SHA256SUMS
          asset: SHA256SUMS
          algorithm: sha256
