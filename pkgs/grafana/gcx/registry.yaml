# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: grafana
    repo_name: gcx
    description: A CLI for managing Grafana Cloud resources. Optimized for agentic usage
    version_constraint: "false"
    version_overrides:
      - version_constraint: Version == "v0.2.0"
        type: http
        url: https://gh-proxy.org/github.com/grafana/gcx/releases/download/{{.Version}}/gcx_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: gcx_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        replacements:
          amd64: x86_64
          darwin: Darwin
          linux: Linux
          windows: Windows
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/grafana/gcx/releases/download/{{.Version}}/gcx_checksums.txt
          asset: gcx_checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/grafana/gcx/releases/download/{{.Version}}/gcx_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/grafana/gcx/releases/download/{{.Version}}/gcx_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: gcx_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/grafana/gcx/releases/download/{{.Version}}/gcx_{{trimV .Version}}_checksums.txt
          asset: gcx_{{trimV .Version}}_checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/grafana/gcx/releases/download/{{.Version}}/gcx_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
