# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    repo_owner: nucleuscloud
    repo_name: neosync
    description: Open source data anonymization and synthetic data orchestration for developers. Create high fidelity synthetic data and sync it across your environments
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 0.3.25")
        type: http
        url: https://gh-proxy.org/github.com/nucleuscloud/neosync/releases/download/{{.Version}}/neosync_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: neosync_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/nucleuscloud/neosync/releases/download/{{.Version}}/checksums.txt
          asset: checksums.txt
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/nucleuscloud/neosync/releases/download/{{.Version}}/neosync_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/nucleuscloud/neosync/releases/download/{{.Version}}/neosync_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        asset: neosync_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
        format: tar.gz
        checksum:
          type: http
          url: https://gh-proxy.org/github.com/nucleuscloud/neosync/releases/download/{{.Version}}/neosync_{{trimV .Version}}_SHA256SUMS
          asset: neosync_{{trimV .Version}}_SHA256SUMS
          algorithm: sha256
        overrides:
          - goos: windows
            type: http
            url: https://gh-proxy.org/github.com/nucleuscloud/neosync/releases/download/{{.Version}}/neosync_{{trimV .Version}}_{{.OS}}_{{.Arch}}.{{.Format}}
            format: zip
