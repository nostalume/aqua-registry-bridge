# yaml-language-server: $schema=https://raw.githubusercontent.com/aquaproj/aqua/main/json-schema/registry.json
packages:
  - type: github_release
    name: kubernetes-sigs/cri-tools/crictl
    repo_owner: kubernetes-sigs
    repo_name: cri-tools
    asset: crictl-{{.Version}}-{{.OS}}-{{.Arch}}.tar.gz
    description: CLI and validation tools for Kubelet Container Runtime Interface (CRI)
    files:
      - name: crictl
    supported_envs:
      - darwin
      - linux
      - amd64
    rosetta2: true
    version_constraint: "false"
    version_overrides:
      - version_constraint: semver("<= 1.23.0")
        type: http
        url: https://gh-proxy.org/github.com/kubernetes-sigs/cri-tools/releases/download/{{.Version}}/crictl-{{.Version}}-{{.OS}}-{{.Arch}}.tar.gz
        checksum:
          type: http
          url: "{{.AssetURL}}.sha256"
          asset: "{{.Asset}}.sha256"
          algorithm: sha256
      - version_constraint: "true"
        type: http
        url: https://gh-proxy.org/github.com/kubernetes-sigs/cri-tools/releases/download/{{.Version}}/crictl-{{.Version}}-{{.OS}}-{{.Arch}}.tar.gz
        checksum:
          type: http
          url: "{{.AssetURL}}.sha512"
          asset: "{{.Asset}}.sha512"
          algorithm: sha512
